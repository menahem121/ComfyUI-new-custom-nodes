# --- Core build ---
cmake

# --- Numeric / SciPy stack (Py3.12-safe; avoids NumPy 2.x breakages) ---
numpy>=1.26
scipy>=1.11.4
numba>=0.60.0
scikit-image>=0.22.0
scikit-learn>=1.4
joblib>=1.3

# --- Imaging ---
Pillow>=10.3.0
imageio[ffmpeg]>=2.31
piexif
pilgram
color-matcher
mss

# --- Computer Vision (choose ONE OpenCV; using contrib+headless for servers) ---
opencv-contrib-python-headless>=4.7.0

# --- DL / Models ---
transformers>=4.41.0
timm>=0.4.12
fairscale>=0.4.4
huggingface_hub>=0.19.0
tqdm
matplotlib
dill
aenum
gitpython

# --- SAM / detection ---
segment-anything>=1.0
groundingdino-py>=0.4.0

# --- ONNX (GPU build only) ---
onnxruntime-gpu>=1.15.0
protobuf>=3.20.2,<6.0.0

# --- Config utils (SAM2 deps) ---
hydra-core>=1.3.0
omegaconf>=2.3.0
iopath>=0.1.9

# --- Background removal (GPU variant) ---
rembg[gpu]
transparent-background>=1.1.2

# --- Custom nodes / repos ---
git+https://github.com/ltdrdata/img2texture.git
git+https://github.com/ltdrdata/cstr
git+https://github.com/ltdrdata/ffmpy.git
git+https://github.com/cozy-comfyui/cozy_comfyui@main#egg=cozy_comfyui
git+https://github.com/cozy-comfyui/cozy_comfy@main#egg=cozy_comfy
